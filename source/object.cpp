//=======================INCLUDE THIS FILE AFTER INCLUSION OF GLM AND GLUT=============================
/*
class Mesh {
public:
	GLuint shaderID;
	GLuint vboID;

	glm::mat4 modelMat;
	glm::mat4 MVP; 

	void calcMVP(glm::mat4 viewMatrix, glm::mat4 perspectiveMatrix) {
		MVP = modelMat * viewMatrix * perspectiveMatrix;
	}

	void rotate(float theta, glm::vec3 axis) {
		modelMat = glm::rotate(&modelMat, theta, axis);
	}
};*/